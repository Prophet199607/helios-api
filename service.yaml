apiVersion: v1
kind: Service
metadata:
  name: helios-api-app-service
spec:
  selector:
    app: helios-api-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  type: LoadBalancer
